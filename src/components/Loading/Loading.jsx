import React from "react";

// This component displays a loading message while data is being fetched or processed.
const Loading = () => {
    return <div>Loading...</div>;
};

export default Loading;
