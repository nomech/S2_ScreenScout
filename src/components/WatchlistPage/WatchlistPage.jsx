import WatchlistGrid from "../WatchlistGrid/WatchlistGrid";

// This component renders the watchlist page, which displays a grid of items in the user's watchlist.
const WatchlistPage = () => {
    return <WatchlistGrid />;
};

export default WatchlistPage;
